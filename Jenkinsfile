pipeline {
    agent any

    stages {
        stage('Run Python') {
            steps {
                echo "Running Python script..."
                bat 'python app.py'
            }
        }
    }
}
